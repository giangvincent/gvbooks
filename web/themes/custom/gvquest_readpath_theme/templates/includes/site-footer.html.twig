<footer class="sana-footer">
  <div class="sana-footer__inner">
    <div>
      <div class="sana-footer__brand">{{ site_name|default('GVQuest Studio') }}</div>
      <p class="mt-3 text-sana-muted/80">{{ 'Elevating stories through immersive reading experiences.'|t }}</p>
    </div>
    <div>
      <div class="text-sana-text font-semibold mb-2">{{ 'Email'|t }}</div>
      <p class="text-sana-text/80">contact@example.com</p>
      <p class="text-sana-muted">Mon–Fri • 9am–5pm</p>
    </div>
    <div>
      <div class="text-sana-text font-semibold mb-2">{{ 'Phone'|t }}</div>
      <p class="text-sana-text/80">+1 (555) 123-4567</p>
      <p class="text-sana-muted">Call or WhatsApp</p>
    </div>
    <div class="flex flex-col gap-2 text-sana-muted">
      <a href="#" class="hover:text-sana-accent">{{ 'About us'|t }}</a>
      <a href="#" class="hover:text-sana-accent">{{ 'Pricing'|t }}</a>
      <a href="#" class="hover:text-sana-accent">{{ 'Contact'|t }}</a>
    </div>
  </div>
  <div class="sana-container flex flex-col gap-2 pb-8 text-xs text-sana-muted/70 md:flex-row md:items-center md:justify-between">
    <div>&copy; {{ "now"|date("Y") }} {{ site_name|default('GVQuest Studio') }}. {{ 'All rights reserved.'|t }}</div>
    <div>{{ 'Crafted with passion in Drupal.'|t }}</div>
  </div>
</footer>
